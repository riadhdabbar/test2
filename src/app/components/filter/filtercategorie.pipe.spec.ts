import { FiltercategoriePipe } from './filtercategorie.pipe';

describe('FiltercategoriePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercategoriePipe();
    expect(pipe).toBeTruthy();
  });
});
